% Figure 5 — HEPI vs activation degree at n=2^15, the only parameter set
% that supports this architecture's full multiplicative depth (degree-7);
% n=2^14 is also viable but only at reduced degree-3 depth -- Sec. IV-D
% (sec:ckks-depth). All bars measured (single real run each); DP-SGD is
% degree-independent.
\begin{tikzpicture}
\begin{axis}[
  ybar,
  width=0.95\columnwidth,
  height=5.5cm,
  bar width=0.55cm,
  ymin=0.0, ymax=0.95,
  ylabel={\HEPI{} Score (measured, $n=2^{15}$)},
  symbolic x coords={Lee proxy deg3, HE-MedInfer deg7, DP-SGD},
  xtick=data,
  xticklabels={Deg.\ 3 (Lee), Deg.\ 7 (ours), DP-SGD},
  x tick label style={font=\scriptsize, rotate=15, anchor=east},
  yticklabel style={font=\scriptsize},
  ylabel style={font=\scriptsize},
  nodes near coords,
  nodes near coords style={font=\tiny, /pgf/number format/.cd, fixed, precision=3},
  legend pos=north east,
  legend style={font=\scriptsize},
  grid=major,
  grid style={dashed, gray!30},
]
\addplot+[fill=blue!25] coordinates {
    (Lee proxy deg3, 0.8297)
    (HE-MedInfer deg7, 0.8297)
    (DP-SGD, 0.5895)
  };
\addplot[gray, dashed, thick, forget plot]
  coordinates {(Lee proxy deg3,0.6) (DP-SGD,0.6)};
\node[gray, font=\tiny, anchor=south west] at (axis cs:Lee proxy deg3,0.62)
  {non-HE cap $\beta+\gamma=0.6$};
\end{axis}
\end{tikzpicture}
